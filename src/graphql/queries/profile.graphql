query Profile($name: String!) {
   profile(name: $name) {
      id
      name
      postsCount
      followersCount
      followingCount
      posts {
         id
         title
         image
      }
   }
}
