query GetAllPosts($input: GetAllPostsInput!) {
   allPosts(input: $input) {
      id
      title
      image
      likeCount
      likeStatus
      author {
         name
      }
      createdAt
      commentsCount
   }
}
